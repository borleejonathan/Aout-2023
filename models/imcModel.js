class User {
    constructor(date, hauteur, poids, imc) {
        this.date = date;
        this.hauteur = hauteur; 
        this.poids = poids;
        this.imc = imc;
    }
}

module.exports = User;